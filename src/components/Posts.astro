---
import Post from "./Post.astro";
const { allPosts } = Astro.props;
---

<main class="flex flex-wrap items-center justify-start gap-8">
	{allPosts.toReversed().map((postData) => <Post post={postData} />)}
</main>
